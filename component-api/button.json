{
  "id": "button",
  "summary": "A standard clickable button, available in several visual styles like primary, ghost, and icon-only.",
  "tagName": "moz-button",
  "description": "A button with multiple types and three sizes.",
  "implementation": {
    "kind": "modern",
    "file": "toolkit/content/widgets/moz-button/moz-button.mjs",
    "codeConnect": "toolkit/content/widgets/moz-button/moz-button.figma.ts"
  },
  "attributes": [
    {
      "name": "label",
      "type": {
        "text": "string"
      },
      "description": "The button's label, will be overridden by slotted content.",
      "figmaConfig": true
    },
    {
      "name": "type",
      "type": {
        "text": "'default' | 'primary' | 'destructive' | 'ghost' | 'icon' | 'icon ghost' | 'muted' | 'icon muted' | 'split' | 'menuitem' | 'toolbar'"
      },
      "description": "The button type. The legal set is asserted by this contract, not read from source: Lit declares only `{ type: String }`, so the values live in moz-button.css's own selectors. `primary`, `destructive` and `split` are matched exactly; `ghost`, `icon`, `muted`, `menuitem` and `toolbar` are matched as words, which is what makes a compound like `icon ghost` work. `menuitem` and `toolbar` are context declarations set by the surrounding component rather than style choices. `default` has no rule of its own, it is the unstyled baseline set in the constructor.\n\nTwo values Figma Code Connect still emits are not in this set. `neutral` and `icon neutral` match no CSS rule at all: the design system renamed that variant to `muted`, which is what both moz-button.css and Figma's own variables now say, and moz-button.figma.ts was never updated. `icon primary` matches only `[type~=\"icon\"]`, because `[type=\"primary\"]` is an exact match a compound cannot satisfy, so it renders as a plain icon button with no primary colour.",
      "figmaConfig": true,
      "default": "default"
    },
    {
      "name": "size",
      "type": {
        "text": "'default' | 'small' | 'large'"
      },
      "description": "The button size. Options: `default`, `small`, `large`.",
      "figmaConfig": true,
      "default": "default"
    },
    {
      "name": "disabled",
      "type": {
        "text": "boolean"
      },
      "description": "The disabled state.",
      "figmaConfig": true,
      "default": "false"
    },
    {
      "name": "title",
      "type": {
        "text": "string"
      },
      "description": "The button's title attribute, used in shadow DOM and therefore not as an attribute on moz-button.",
      "figmaConfig": false
    },
    {
      "name": "tooltipText",
      "type": {
        "text": "string"
      },
      "description": "Set the title property; the title attribute will be used first.",
      "figmaConfig": false
    },
    {
      "name": "ariaLabel",
      "type": {
        "text": "string"
      },
      "description": "The button's aria-label attribute, used in shadow DOM and therefore not as an attribute on moz-button.",
      "figmaConfig": false
    },
    {
      "name": "ariaHasPopup",
      "type": {
        "text": "string"
      },
      "description": "The button's aria-haspopup attribute, indicates a popup element can be triggered by the button.",
      "figmaConfig": false
    },
    {
      "name": "ariaExpanded",
      "type": {
        "text": "string"
      },
      "description": "The button's aria-expanded attribute, indicates whether the controlled elements are displayed or hidden.",
      "figmaConfig": false
    },
    {
      "name": "ariaPressed",
      "type": {
        "text": "string"
      },
      "description": "The button's aria-pressed attribute, used in shadow DOM and therefore not as an attribute on moz-button.",
      "figmaConfig": false
    },
    {
      "name": "ariaChecked",
      "type": {
        "text": "string"
      },
      "description": "The button's aria-checked attribute, used in shadow DOM and therefore not as an attribute on moz-button.",
      "figmaConfig": false
    },
    {
      "name": "iconSrc",
      "type": {
        "text": "string"
      },
      "description": "Path to the icon that should be displayed in the button.",
      "figmaConfig": true
    },
    {
      "name": "attention",
      "type": {
        "text": "boolean"
      },
      "description": "Show a dot notification on the button if true.",
      "figmaConfig": true,
      "default": "false"
    },
    {
      "name": "iconPosition",
      "type": {
        "text": "'start' | 'end'"
      },
      "description": "The icon's position relative to the button label. Options: `start`, `end`.",
      "figmaConfig": true,
      "default": "start"
    },
    {
      "name": "menuId",
      "type": {
        "text": "string"
      },
      "description": "A CSS selector string that identifies the associated panel-list menu element controlled by the button.",
      "figmaConfig": true
    },
    {
      "name": "parentDisabled",
      "type": {
        "text": "boolean"
      },
      "description": "When the parent of this component is disabled.",
      "figmaConfig": false
    },
    {
      "name": "accessKey",
      "type": {
        "text": "string"
      },
      "description": "Internal, maps the accesskey attribute to the accessKey JS property (mapped, not in the top JSDoc block but declared in static properties).",
      "figmaConfig": false
    },
    {
      "name": "hasVisibleLabel",
      "type": {
        "text": "boolean"
      },
      "description": "Internal, tracks whether or not the button has a visible label (state property, not a real attribute).",
      "figmaConfig": false
    }
  ],
  "slots": [
    {
      "name": "(default)",
      "description": "The button's content, overrides label property."
    }
  ],
  "events": [
    {
      "name": "click",
      "description": "The click event."
    }
  ],
  "cssProperties": [
    {
      "name": "--button-outer-padding-inline",
      "description": "Used to set the outer inline padding of toolbar style buttons.",
      "default": "var(--toolbarbutton-padding-outer)"
    },
    {
      "name": "--button-outer-padding-block",
      "description": "Used to set the outer block padding of toolbar style buttons.",
      "default": "4px"
    },
    {
      "name": "--button-outer-padding-inline-start",
      "description": "Used to set the outer inline-start padding of toolbar style buttons.",
      "default": "var(--button-outer-padding-inline, 0)"
    },
    {
      "name": "--button-outer-padding-inline-end",
      "description": "Used to set the outer inline-end padding of toolbar style buttons.",
      "default": "var(--button-outer-padding-inline, 0)"
    },
    {
      "name": "--button-outer-padding-block-start",
      "description": "Used to set the outer block-start padding of toolbar style buttons.",
      "default": "var(--button-outer-padding-block, 0)"
    },
    {
      "name": "--button-outer-padding-block-end",
      "description": "Used to set the outer block-end padding of toolbar style buttons.",
      "default": "var(--button-outer-padding-block, 0)"
    }
  ],
  "variants": [
    {
      "axis": "type",
      "values": [
        "default",
        "primary",
        "destructive",
        "ghost",
        "icon",
        "icon ghost",
        "muted",
        "icon muted",
        "split"
      ]
    },
    {
      "axis": "size",
      "values": [
        "default",
        "small",
        "large"
      ]
    },
    {
      "axis": "iconPosition",
      "values": [
        "start",
        "end"
      ]
    }
  ],
  "usage": "<moz-button type=\"primary\">Save</moz-button>",
  "cssParts": [
    {
      "name": "button",
      "description": "The internal <button> element."
    },
    {
      "name": "chevron-button",
      "description": "The dropdown-opening chevron on a split-type button."
    },
    {
      "name": "moz-button-label",
      "description": "The button's internal label element."
    }
  ]
}
